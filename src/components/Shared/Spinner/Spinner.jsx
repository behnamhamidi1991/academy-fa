import React from "react";
import spinner from "../../../assets/loadSpinner.svg";

const Spinner = () => {
  return (
    <div>
      <img src={spinner} alt="" />
    </div>
  );
};

export default Spinner;
